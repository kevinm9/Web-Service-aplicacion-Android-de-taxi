<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.login">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".confirmarviaje"></activity>
        <activity android:name=".finalizarviaje" />
        <activity android:name=".ruta" />
        <activity android:name=".carro" />
        <activity android:name=".viajar" />
        <activity android:name=".buscarviaje" />
        <activity android:name=".menuchofer" />
        <activity android:name=".menucliente" />
        <activity android:name=".menusuario" />
        <activity android:name=".registrarse" />
        <activity android:name=".menu">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>